jenkins:
  systemMessage: "Welcome to Jenkins configured with from JCASC Configs! *** new_configs *** poi" # Custom system message

  securityRealm:
    local:
      allowsSignup: false
      users:
      - id: "admin"
        password: "admin123"
        name: "Administrator"
    
  authorizationStrategy:
    matrix:
      permissions:
        - "Overall/Read:anonymous"
        - "Overall/Administer:admin"
        - "Overall/Read:authenticated"
        - "Agent/Build:authenticated"
        - "Agent/Configure:authenticated"
        - "Agent/Connect:authenticated"
        - "Agent/Create:authenticated"
        - "Agent/Delete:authenticated"
        - "Agent/Disconnect:authenticated"
        - "Credentials/Create:authenticated"
        - "Credentials/Delete:authenticated"
        - "Credentials/ManageDomains:authenticated"
        - "Credentials/Update:authenticated"
        - "Credentials/View:authenticated"
        - "Job/Build:authenticated"
        - "Job/Cancel:authenticated"
        - "Job/Configure:authenticated"
        - "Job/Create:authenticated"
        - "Job/Delete:authenticated"
        - "Job/Discover:authenticated"
        - "Job/Move:authenticated"
        - "Job/Read:authenticated"
        - "Job/Workspace:authenticated"
        - "Run/Delete:authenticated"
        - "Run/Replay:authenticated"
        - "Run/Update:authenticated"
        - "View/Configure:authenticated"
        - "View/Create:authenticated"
        - "View/Delete:authenticated"
        - "View/Read:authenticated"

  jobs:
    - script: >
        job('Seed Job') {
          scm {
            git {
              remote {
                url 'https://github.com/your-repo/your-repo.git'
                credentialsId 'your-credentials-id'
              }
              branch 'main'
            }
          }
          steps {
            jobDsl {
              targets 'jobs/**/*.groovy'
            }
          }
        }

  git:
    config:
      - key: "user.name"
        value: "Your Name"
      - key: "user.email"
        value: "your.email@example.com"